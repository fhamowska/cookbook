{% extends 'base.html.twig' %}

{% block title %}{{ 'title.register'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'title.register'|trans }}</h1>

    {{ form_start(registrationForm) }}
    {{ form_row(registrationForm.email, {
        label: 'label.email'|trans
    }) }}
    {{ form_row(registrationForm.password, {
        label: 'label.password'|trans
    }) }}

    <button type="submit" class="btn btn-lg btn-primary">{{ 'action.register'|trans }}</button>
    {{ form_end(registrationForm) }}
    <p><a class="btn btn-lg btn-secondary" href="{{ url('app_login') }}" title="{{ 'label.login'|trans }}">
            {{ 'label.login'|trans }}
        </a></p>
{% endblock %}