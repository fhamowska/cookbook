{% include 'default/_form.html.twig' with {
    page_title: 'title.user_delete'|trans({'%id%': user.id}),
    form: form,
    submit_label: 'action.delete'|trans,
    back_to_list_path: 'recipe_index',
} %}